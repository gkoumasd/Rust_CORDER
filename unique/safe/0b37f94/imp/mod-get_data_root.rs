# [doc = " Returns (but **does not create**) path to **top-level** data directory for"] # [doc = " provided data type."] # [doc = ""] # [doc = " A result of `Ok` means that we determined where the data SHOULD go, but"] # [doc = " it DOES NOT guarantee that the directory actually exists. (See"] # [doc = " [`data_root`](fn.data_root.html).)"] pub fn get_data_root (t : AppDataType) -> Result < PathBuf , AppDirsError > { platform :: get_app_dir (t) }
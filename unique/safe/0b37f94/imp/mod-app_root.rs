# [doc = " Creates (if necessary) and returns path to **app-specific** data"] # [doc = " directory for provided data type."] # [doc = ""] # [doc = " If the directory structure does not exist, this function will recursively"] # [doc = " create the full hierarchy. Therefore, a result of `Ok` guarantees that the"] # [doc = " returned path exists."] pub fn app_root (t : AppDataType , app : & AppInfo) -> Result < PathBuf , AppDirsError > { let path = try ! (get_app_root (t , app)) ; match fs :: create_dir_all (& path) { Ok (..) => Ok (path) , Err (e) => Err (e . into ()) , } }
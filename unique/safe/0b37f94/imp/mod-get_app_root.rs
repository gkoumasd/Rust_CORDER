# [doc = " Returns (but **does not create**) path to **app-specific** data directory"] # [doc = " for provided data type."] # [doc = ""] # [doc = " A result of `Ok` means that we determined where the data SHOULD go, but"] # [doc = " it DOES NOT guarantee that the directory actually exists. (See"] # [doc = " [`app_root`](fn.app_root.html).)"] pub fn get_app_root (t : AppDataType , app : & AppInfo) -> Result < PathBuf , AppDirsError > { if app . author . is_empty () || app . name . is_empty () { return Err (AppDirsError :: InvalidAppInfo) ; } data_root (t) . map (| mut root | { if platform :: USE_AUTHOR { root . push (utils :: sanitized (app . author)) ; } root . push (utils :: sanitized (app . name)) ; root }) }